= render 'header'
.main
  .card-display
    %ul.card-display__box
      %li
        %a{href: "#content01"} 新着文書
      %li
        %a{href: "#content02"} 自己啓発
      %li
        %a{href: "#content03"} 参考書
      %li
        %a{href: "#content04"} 経済
      %li
        %a{href: "#content05"} 小説
    .contents
      #content01
        .ProductLists
          - @cards.each do |card|
            .ProductList
              =link_to card_path(card.id), class: 'ProductList__body' do
                .type
                  = card.type.name
                .name
                  = card.title
                .user
                  = icon('fas', 'user')
                  = card.user.name


      #content02
        .ProductLists
          - @cards_i.each do |card|
            .ProductList
              =link_to card_path(card.id), class: 'ProductList__body' do
                .type
                  = card.type.name
                .name
                  = card.title
                .user
                  = icon('fas', 'user')
                  = card.user.name
      #content03
        .ProductLists
          - @cards_t.each do |card|
            .ProductList
              =link_to card_path(card.id), class: 'ProductList__body' do
                .type
                  = card.type.name
                .name
                  = card.title
                .user
                  =icon('fas', 'user')
                  = card.user.name
      #content04
        .ProductLists
          - @cards_p.each do |card|
            .ProductList
              =link_to card_path(card.id), class: 'ProductList__body' do
                .type
                  = card.type.name
                .name
                  = card.title
                .user
                  =icon('fas', 'user')
                  = card.user.name
      #content05
        .ProductLists
          - @cards_g.each do |card|
            .ProductList
              =link_to card_path(card.id), class: 'ProductList__body' do
                .type
                  = card.type.name
                .name
                  = card.title
                .user
                  =icon('fas', 'user')
                  = card.user.name

.footer
  BookーShelf